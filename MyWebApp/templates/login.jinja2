{#LOGIN HTML#}

{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}
{{ form.csrf_token }} 

{%block content%}
<div class="login-page">
    <h1 style="font-size: 200%; font-variant-caps: normal; color:purple" id="header" class="display-3 text-center">Sign in</h1>
    <p></p>
     <div class="form">
        <form class="form-signin" method="POST" action="/login">
        {{ form.hidden_tag() }}
        {{ wtf.form_field(form.email) }}
        {{ wtf.form_field(form.password) }}
        <button type="submit">login</button>
        <p class="message">not signed in? <a  href="{{ url_for('register')}}">register</a></p>
    </form>
  </div>
</div>
{%endblock%}

